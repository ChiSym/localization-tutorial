[tool.poetry]
name = "genjax-localization-tutorial"
version = "0.1.0"
description = ""
authors = ["Matthew Huebert <mhuebert@gmail.com>"]
readme = "README.md"

package-mode = false

[tool.poetry.dependencies]
python = ">=3.11,<3.13"
jupytext = "^1.16.1"
genjax = "0.8.2.dev1"
genstudio = "2025.2.2"

[tool.pyright]
venvPath = "."
venv = ".venv"
include = ["genjax-localization-tutorial"]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
